---
title: Apache Kafka and the File System
source: https://docs.confluent.io/kafka/design/file-system-constant-time.html#why-the-file-system
author:
published: 2026-07-09
created: 2026-07-09
description: Describes why Apache Kafka uses the file system and the performance boosts that the file system offers
tags:
  - clippings
---
## Kafka and the File System

Apache Kafka® is an open-source distributed streaming system used for stream processing, real-time data pipelines, and data integration at scale.

This topic describes how and why Kafka relies on the file system for storing and caching messages.

## Why the file system?

A key reason for Kafka’s reliance on the file system is that hard drive throughput has been diverging from disk seek latency for the several years. For example, a [JBOD](http://en.wikipedia.org/wiki/Non-RAID_drive_architectures) configuration with six 7200rpm SATA RAID-5 array:

- Performs linear writes at about 600 MBps.
- Performs random writes at about 100 Kbps, over 6000 times slower than linear writes

Linear reads and writes provide a predictable usage pattern, and are heavily optimized by the operating system. A modern operating system provides read-ahead and write-behind techniques that prefetch data in large block multiples and group smaller logical writes into large physical writes for these linear operations. To learn more, read the [ACM Queue article](http://queue.acm.org/detail.cfm?id=1563874). Of note, the article authors find that [sequential disk access can in some cases be faster than random memory access](http://deliveryimages.acm.org/10.1145/1570000/1563874/jacobs3.jpg).

Because of the performance divergence between throughput and disk seeks, modern operating systems have become increasingly aggressive in their use of main memory for disk caching. A modern OS will divert *all* free memory to disk caching with little performance penalty when the memory is reclaimed. All disk reads and writes will go through this unified cache. This feature is hard to turn off without using direct I/O, so even if a process maintains an in-process cache of the data, this data will likely be duplicated in the OS’s page cache, effectively storing everything twice.

## Kafka and the JVM

Kafka is built on top of the JVM, and Java memory usage has the following characteristics:

1. The memory overhead of objects is very high, often doubling the size of the data stored (or worse).
2. Java garbage collection becomes increasingly slow as the in-heap data increases.

As a result, using the file system and relying on the kernel page cache is better than an in-memory cache or other structure. The page cache provides at least double the available cache by providing automatic access to all free memory, and likely double again by storing a compact byte structure rather than objects.

For example, by using the file system, a 32 GB machine can have a cache of up to 28-30GB, without GC penalties. Furthermore, this cache will stay warm even if the service is restarted, whereas the in-process cache will need to be rebuilt in memory, which takes a long time.

With Kafka, instead of maintaining as much as possible and flushing it to the file system when memory space runs out, all data is immediately written to a persistent log on the file system without flushing to disk. This means the data is essentially transferred into the kernel’s page cache. All logic for maintaining coherency between the cache and file system is in the OS, which tends to do so more efficiently and more correctly than one-off in-process attempts.

## Persistent queue and constant time

A typical messaging system might use per-consumer queues with associated BTrees or similar data structures to maintain message metadata. BTrees are a versatile data structure, but they come with a fairly high cost; Btree operations are O(log N), and O(log N), which is typically considered equivalent to constant time.

In contrast, disk operations are slower. Disk seeks are typically 10 MS each, and each disk can do only one seek at a time, so parallelism is limited. This means that even 5-6 disk seeks leads to a lot of overhead. Since storage systems mix fast cached operations with slow physical disk operations, as data increases with a fixed cache, the performance of tree structures is often superlinear, meaning doubling your data makes things worse than twice as slow.

Instead, a persistent queue could be built on simple reads and appends to files like you typically see with logging solutions. Then all operations are O(1) and reads do not block writes or each other. This offers performance advantages because performance is decoupled from the data size. One server can use a number of cheap, low-rotational speed 1+TB SATA drives. They have poor seek performance, but these drives perform acceptably for large reads and writes and are inexpensive.

Kafka has access to virtually unlimited disk space without any performance penalty, which means that Kafka can provide some features not usually found in a messaging system. For example, in Kafka, instead of attempting to delete messages as soon as they are consumed, Kafka can retain messages for a relatively long period (a week, for example), which enables flexibility for consumers.

## Related content

- [Kafka Producer Design](https://docs.confluent.io/kafka/design/producer-design.html#producer-design)
- [Kafka Consumer Design: Consumers, Consumer Groups, and Offsets](https://docs.confluent.io/kafka/design/consumer-design.html#consumer-design)
- [Kafka Message Delivery Guarantees](https://docs.confluent.io/kafka/design/delivery-semantics.html#delivery-semantics)
- [Kafka Log Compaction](https://docs.confluent.io/kafka/design/log_compaction.html#log-compaction)
- [Kafka Replication and Committed Messages](https://docs.confluent.io/kafka/design/replication.html#replication)
- [Kafka Quotas](https://docs.confluent.io/kafka/design/quotas.html#quotas)
  
  
  ---
  ## Explanations

Выделенный текст объясняет, **почему Apache Kafka активно использует файловую систему** для хранения данных, вместо того чтобы полагаться исключительно на собственный кэш в оперативной памяти (RAM).

Вот основные аргументы, описанные в этом фрагменте:

- **Огромная разница между последовательным и случайным доступом к диску:** Производительность жестких дисков сильно зависит от того, как именно записываются данные. Последовательная (линейная) запись происходит очень быстро (в примере указано около 600 МБ/с). Однако случайная запись работает более чем в 6000 раз медленнее (около 100 КБ/с) из-за физических задержек на поиск нужного сектора на диске.
    
- **Мощная оптимизация операционными системами (ОС):** Современные ОС отлично оптимизируют линейные операции чтения и записи. Они умеют заранее считывать данные крупными блоками (read-ahead) и группировать мелкие команды записи в одну большую (write-behind). Из-за этого последовательное чтение с диска иногда может быть даже быстрее, чем случайное чтение из оперативной памяти.
    
- **Эффективное использование кэша ОС (Page Cache):** Поскольку доступ к дискам требует оптимизации, современные ОС автоматически забирают всю свободную оперативную память под кэширование дисковых операций. Если бы Kafka создала свой собственный внутренний кэш в памяти, эти же данные все равно автоматически попали бы в кэш ОС при записи на диск. Это привело бы к дублированию — одни и те же данные хранились бы в оперативной памяти дважды.
    

**Вкратце:** Kafka спроектирована так, чтобы записывать данные на диск последовательно (что делает работу с диском очень быстрой) и полностью полагаться на встроенный кэш операционной системы, избегая двойного расхода памяти.

---

  Выделенный текст объясняет, почему Apache Kafka предпочитает использовать кэш страниц операционной системы (OS page cache) вместо того, чтобы создавать собственный кэш в памяти (in-memory cache) внутри виртуальной машины Java (JVM).

Вот основные аргументы, описанные в этом фрагменте:

- **Проблемы с памятью в Java (JVM):**
    
    1. Java-объекты потребляют очень много памяти из-за накладных расходов (часто увеличивая реальный размер данных вдвое или даже больше).
        
    2. Чем больше данных хранится в оперативной памяти приложения (в куче/heap), тем медленнее начинает работать сборщик мусора (Garbage Collector). Это приводит к падению производительности.
        
- **Преимущества кэша операционной системы:** Полагаться на встроенный кэш ОС гораздо эффективнее. ОС автоматически использует всю свободную оперативную память сервера для кэширования дисковых операций. Поскольку ОС хранит данные в виде компактных массивов байтов, а не "тяжелых" объектов Java, в ту же память помещается значительно больше полезной информации.
    
- **Эффективное использование ресурсов без "тормозов":** В примере указано, что на сервере с 32 ГБ оперативной памяти под кэш можно выделить 28–30 ГБ, и при этом приложение не будет страдать от замедлений, связанных со сборкой мусора в Java.
    
- **Сохранение кэша при перезапуске:** Если сервис Kafka потребуется перезапустить, кэш операционной системы останется "прогретым" (данные по-прежнему будут доступны в оперативной памяти). Если бы Kafka использовала внутренний кэш приложения, после рестарта его пришлось бы долго восстанавливать с диска.
    
- **Упрощение архитектуры:** Kafka не пытается накапливать данные в памяти приложения, чтобы потом разом сбросить их на диск. Вместо этого все данные сразу отправляются в файл журнала на диске, что фактически помещает их напрямую в кэш ОС. Операционная система сама заботится о том, как и когда физически записать эти данные на жесткий диск, делая это намного эффективнее и надежнее, чем если бы эту логику пытались реализовать внутри самой Kafka.
    

**Вкратце:** Kafka делегирует работу с оперативной памятью на уровень операционной системы, избегая недостатков платформы Java. Это делает систему более быстрой, стабильной и позволяет ей эффективно обрабатывать огромные объемы данных.

---
Выделенный фрагмент описывает альтернативный, высокоэффективный подход Apache Kafka к хранению данных, который противопоставляется традиционным системам обмена сообщениями, использующим сложные древовидные структуры (например, B-деревья).

Вот основные идеи этого отрывка:

- **Простой журнальный подход (Log-based):** Вместо построения сложных индексов, Kafka организует постоянную очередь в виде обычного журнала. Новые сообщения просто дописываются в конец файла (append), а чтение происходит последовательно.
    
- **Константное время выполнения ($O(1)$):** Благодаря линейной записи, время выполнения любой операции чтения или записи всегда одинаково и минимально (сложность $O(1)$). При этом процессы чтения не блокируют ни запись новых данных, ни работу других читающих процессов.
    
- **Независимость производительности от объема:** В традиционных базах данных с ростом объема информации поиск начинает работать медленнее. В Kafka производительность полностью «отвязана» от размера данных — система будет работать одинаково быстро как с мегабайтом, так и с терабайтом логов.
    
- **Использование дешевого оборудования:** Поскольку линейные чтение и запись не требуют быстрого случайного поиска по диску (seek time), для серверов Kafka отлично подходят дешевые и вместительные (от 1 ТБ) жесткие диски SATA с низкой скоростью вращения шпинделя. Они плохо ищут случайные блоки данных, но прекрасно и недорого справляются с последовательной записью.
    

**Вкратце:** Архитектура Kafka опирается на последовательную запись файлов (как в обычных логах). Это позволяет системе сохранять максимальную скорость работы независимо от накопленного объема данных и существенно экономить на инфраструктуре, используя дешевые жесткие диски большой емкости.